services:
    sqwok:
        image: local/sqwok:latest
        build: .
        environment:
            COLORTERM: truecolor # remove this if your host terminal does not support 24-bit color
            SQWOK_SERVER: https://sqwok.fixbase.io
        volumes:
            - sqwok-data:/home/sqwok/.sqwok
        read_only: true
        cap_drop:
            - ALL
        security_opt:
            - no-new-privileges:true

volumes:
    sqwok-data:
